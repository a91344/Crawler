import criterion.Crawler;
import criterion.Pipeline;
import org.jetbrains.annotations.NotNull;

public class XsbiqugePipeline implements Pipeline {
    @Override
    public void progress(@NotNull Crawler crawler) {

    }
}
